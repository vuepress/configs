import Vue from 'vue'

let foo_bar = "foo_bar"

const FooBar = "FooBar"

const FOO_BAR = "FOO_BAR"

const fooBar = "fooBar"

const __FOO_BAR__ = '__FOO_BAR__'

export default Vue.extend({
  name: 'bar',
})
